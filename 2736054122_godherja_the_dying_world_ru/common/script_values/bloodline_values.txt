trait_counter = {
    value = 0
    if = {
        limit = { has_trait = bloodline_cenware }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_nikaryin }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_phanagorax }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_diplomacy_skill }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_martial_skill }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_stewardship_skill }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_intrigue_skill }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_learning_skill }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_dhacixen }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_aexionarax }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_maklea }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_nothyx }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_axiaothea }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_aeschraes }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_pothacleas }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_the_skull_taker }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_gardfrei }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_huegons }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_the_good_king }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_the_prophet }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_the_black_sun }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_kathanouxa }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_odyr_then }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_odyr_lleth }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_odyr_hune }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_vercigex }
        add = 1
    }
	if = {
        limit = { has_trait = bloodline_cois_defeated }
        add = 1
    }
	if = {
        limit = { has_trait = bloodline_cois_victorious }
        add = 1
    }
	if = {
		limit = { has_trait = bloodline_gatecrasher }
		add = 1
	}
}