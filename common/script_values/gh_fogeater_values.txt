#######################
# MONGOL VALUES					###fogeater values
#######################
# Conquering things
# Drinking mare's milk
# Destroying civilisations


# Chance of Mongols appearing per year between 1180 and 1250
scathach_arawn_appearance_base_chance = 1

 Weight used in selecting targets for the automated Mongol invasions ####commented out till dejure setup is done
 fogeater_invasion_target_character_weight = {
	value = 0.001
	every_sub_realm_county = {
		if = {
			limit = {
				title_province = {
						geographical_region = world_ga_mountainheights
				}
			}
			add = 10000
		}
		else_if = {
			limit = {
				title_province = {
					geographical_region = world_ga_mayikprolollan
				}
			}
			add = 1000
		}
		else_if = {
			limit = {
				title_province = {
					geographical_region = world_ga_eastern_clanlands
				}
			}
			add = 100
		}
		else_if = {
			limit = {
				title_province = {
					geographical_region = world_ga_western_clanlands
				}
			}
			add = 10
		}
		else_if = {
			limit = {
				title_province = {
					geographical_region = world_ga_western_aversaria
				}
			}
			add = 1
		}
		else_if = {
			limit = {
				title_province = {
					geographical_region = world_ga_marcher_kingdoms
				}
			}
			add = 0.1
		}
		else_if = {
			limit = {
				title_province = {
					geographical_region = world_ga_aversarian_homelands
				}
			}
			add = 0.01
		}
	}
}

# Mongol breakup succession score				####do we want the empire to break up?
# mongol_breakup_succession_score = {
#	# Prioritize the actual heir
#	if = {
#		limit = {
#			exists = scope:old_mongol_emperor.primary_heir
#			this = scope:old_mongol_emperor.primary_heir
#		}
#		add = 100
#	}
#	# A bonus for being of the same house
#	if = {
#		limit = {
#			exists = house
#			house = scope:old_mongol_emperor.house
#		}
#		add = 50
#	}
#	# Prioritize siblings
#	if = {
#		limit = {
#			is_sibling_of = scope:old_mongol_emperor
#		}
#		add = 100
#	}
#	# Children of children of the old khan gain some priority
#	else_if = {
#		limit = {
#			any_parent = {
#				any_parent = {
#					this = scope:old_mongol_emperor
#				}
#			}
#		}
#		add = 50
#	}
#	else_if = {
#		limit = {
#			is_close_family_of = scope:old_mongol_emperor
#		}
#		add = 30
#	}
#	else_if = {
#		limit = {
#			is_close_or_extended_family_of = scope:old_mongol_emperor
#		}
#		add = 15
#	}
#	# Having more titles gives higher priority
#	every_held_title = {
#		add = {
#			value = tier
#			multiply = 10
#		}
#	}
#
#	# Not being a part of the empire doesn't disqualify you, but puts you at the bottom of the list
#	if = {
#		limit = {
#			NOT = {
#				target_is_liege_or_above = scope:old_mongol_emperor
#			}
#		}
#		multiply = 0.01
#	}
#}